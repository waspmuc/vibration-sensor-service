{
  "swagger": "2.0",
  "info": {
    "version": "0.0.4",
    "title": "Vibration Sensor Service on RaspberryPi"
  },
  "host": "localhost:10020",
  "basePath": "/",
  "schemes": [
    "http",
    "https"
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/data": {
      "get": {
        "description": "Returns current value of the Vibration sensor to the caller",
        "operationId": "sensor",
        "responses": {
          "200": {
            "description": "Success",
            "schema": {
              "$ref": "#/definitions/sensorResponse"
            }
          },
          "default": {
            "description": "Error",
            "schema": {
              "$ref": "#/definitions/ErrorResponse"
            }
          }
        }
      }
    },
    "/swagger": {}
  },
  "definitions": {
    "sensorResponse": {
      "required": [
        "message"
      ],
      "properties": {
        "message": {
          "type": "string"
        }
      }
    },
    "ErrorResponse": {
      "required": [
        "message"
      ],
      "properties": {
        "message": {
          "type": "string"
        }
      }
    }
  }
}